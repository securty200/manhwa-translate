# ── AI Manga Translator — Python Dependencies ──────────────────────────
# Core
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.5.0,<3.0.0

# Database
sqlalchemy[asyncio]>=2.0.35,<3.0.0
aiosqlite>=0.20.0,<1.0.0
alembic>=1.13.0,<2.0.0

# Async
httpx>=0.27.0,<1.0.0
websockets>=13.0,<14.0.0

# Image processing
Pillow>=11.0.0,<12.0.0
opencv-python-headless>=4.10.0,<5.0.0
numpy>=1.26.0,<2.0.0

# OCR
manga-ocr>=0.1.0,<1.0.0        # Japanese manga OCR
# paddleocr>=2.9.0,<3.0.0      # Alternative OCR (uncomment if needed)
# pytesseract>=0.3.10,<1.0.0   # Tesseract integration (uncomment if needed)

# Translation
openai>=1.50.0,<2.0.0           # OpenAI / DeepSeek API
# anthropic>=0.40.0,<1.0.0     # Anthropic Claude (uncomment if needed)
# google-generativeai>=0.8.0    # Google Gemini (uncomment if needed)

# Text Detection
# craft-text-detector>=0.0.1   # CRAFT text detection (uncomment if needed)
# ultralytics>=8.3.0           # YOLO (uncomment if needed)

# Inpainting
# lama-cleaner>=0.3.0           # LaMa (uncomment if needed)
# diffusers>=0.30.0             # Stable Diffusion (uncomment if needed)
# torch>=2.4.0                  # PyTorch (uncomment if needed)

# Caching — Redis async client with C parser (required for L2 cache)
redis[hiredis]>=5.2.0,<6.0.0

# Performance & Monitoring
psutil>=6.1.0,<7.0.0              # System resource monitoring (CPU, RAM)

# GPU Acceleration
# torch>=2.4.0                   # PyTorch (uncomment for CUDA/GPU)
# onnxruntime-gpu>=1.18.0        # GPU-accelerated ONNX (uncomment for CUDA)
# onnxruntime>=1.18.0            # ONNX Runtime (CPU fallback)

# Profiling
# py-spy>=0.3.14                 # Sampling profiler (uncomment for profiling)
# memory-profiler>=0.61.0        # Line-by-line memory profiler

# Utilities
python-multipart>=0.0.12,<1.0.0
python-dotenv>=1.0.0,<2.0.0
aiofiles>=24.1.0,<25.0.0

# Testing
pytest>=8.3.0,<9.0.0
pytest-asyncio>=0.24.0,<1.0.0
pytest-cov>=5.0.0,<6.0.0
httpx>=0.27.0,<1.0.0

# Development
ruff>=0.7.0,<1.0.0
mypy>=1.13.0,<2.0.0
